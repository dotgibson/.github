{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Shared Renovate policy for the dotfiles fleet. Extend it from any repo with `local>dotgibson/.github`.",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests",
    "docker:pinDigests"
  ],
  "labels": ["dependencies"],
  "timezone": "Etc/UTC",
  "schedule": ["after 3am and before 7am on tuesday"],
  "semanticCommits": "enabled",
  "configMigration": true,
  "ignorePaths": [
    "**/node_modules/**",
    "**/bower_components/**",
    "**/vendor/**",
    "**/examples/**",
    "**/__tests__/**",
    "**/test/**",
    "**/tests/**",
    "**/__fixtures__/**",
    "core/**",
    "offensive/companion/**"
  ],
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "packageRules": [
    {
      "description": "The fleet's own reusable workflows ride a moving @v3 major tag advanced by the release process (RELEASE-STRATEGY.md) — Renovate must never pin or bump them.",
      "matchManagers": ["github-actions"],
      "matchDepNames": ["dotgibson/**"],
      "enabled": false
    },
    {
      "description": "Group all third-party GitHub Actions bumps into one weekly PR, committed as `ci(deps):` to match the fleet's convention (replaces the per-repo Dependabot github-actions ecosystem).",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions",
      "semanticCommitType": "ci",
      "semanticCommitScope": "deps"
    },
    {
      "description": "The mise-managed language runtimes (python/java/lua in core/mise/config.toml) are deliberate pins coupled to the audit toolchain (luacheck, etc.). Bump them by hand in dotfiles-core so the change goes through `make audit`, not a Renovate dashboard checkbox — and so a major runtime jump (java 25, lua 5.5, python 3.14) can't fan out to every vendoring repo unreviewed. Disable the manager entirely.",
      "matchManagers": ["mise"],
      "enabled": false
    }
  ]
}
